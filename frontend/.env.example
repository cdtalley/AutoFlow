# Base URL of the AutoFlow FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Match server WEBHOOK_API_KEY so the browser can call POST /webhook and WS /ws (dev only; do not ship secrets in public builds)
# NEXT_PUBLIC_WEBHOOK_API_KEY=
